# Third-review integration repair report

Status: `REPAIRED_AND_DETERMINISTICALLY_VALIDATED`

## Repairs

- TR-01: V4 separates protocol alias, coordinate hash, and full contract ID;
  the frozen registry rejects alias and reverse-coordinate collisions.
- TR-02/TR-03: manifest loading and alignment enforce role-specific row scope,
  exact split/label/`label_known` agreement, known-only supervision/evaluation,
  and per-artifact exclusion reports. Historical `val` is normalized through
  an explicit evidenced provider-token mapping.
- TR-04: the central leakage module emits typed per-target reports and blocks
  atomic overlap, reuse, ordering, metadata, and held-out-coordinate defects.
- TR-05: inference is dataset-stratified; same-numbered cross-dataset seeds are
  rejected as a pooled block. Per-dataset exact tests and intervals precede a
  secondary hierarchical bootstrap.
- TR-06: BCE-surrogate, Brier headline, and selective zero--one risks have
  distinct names in implementation, schemas, documentation, and the paper
  placeholder.
- TR-07: the GraphSafe confidence component is a compatibility component,
  excluded from complete strong baselines and headline Holm families.
- TR-08: tiny V4 CSV/manifests now pass through manifest loading, registry
  binding, scope and known-label filtering, typed leakage, the real
  `--validate-only` runner, exact result-row materialization, and no-training
  gate completeness. Contract IDs retain their hash-bearing form throughout.

## Historical conversion result

The read-only scan found 201 requested-pattern candidates: 196 Elliptic and
five DGraphFin. Seventy Elliptic candidates have historical validation
evidence and pass structural row audits. All 201 lack evidenced role, complete
deployment contract, config/code hashes, compute cost, and compute-cost
provenance, so zero V4 manifests were created.

The 360-cell matrix contains 82 ambiguous-historical cells, 48
metadata-blocked cells, and 230 missing cells. Frozen alias validation passes.
Complete contract bindings, typed cross-role leakage, no-training runner
materialization, and no-training gate completeness cannot run on real
artifacts until loadable, role-specific V4 manifests exist. The 60 required
typed leakage scopes are recorded as blocked before atomic inspection, not as
passes.

No router or learned baseline was fitted, no target metric or oracle was
computed, and no historical prediction was modified.
