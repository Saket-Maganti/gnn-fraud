# CoReGraph second-review pilot-semantics repair report

Verdict:
`COREGRAPH_PILOT_SEMANTICS_CLOSED_READY_FOR_MANIFEST_CONVERSION_REVIEW`.

All four no-heavy draft-PR checks pass on validated repair commit
`c81bb7ff0397c129eaf7845adf8e4b08fe61d67f`.

The thirteen second-review findings are repaired in deterministic code,
regression tests, frozen schemas, theory scope, documentation, and paper
placeholders. No empirical evidence was created or connected.

## Scientific closure

- Pilot evaluation consumes the exact frozen abstention decision and records
  its threshold, provenance, capacity, cost, counts, and checksum. Zero
  coverage has undefined selective risk and cannot win the gate.
- Headline regret uses one whole-contract feasible expert. The per-instance
  clairvoyant oracle remains a separate diagnostic ceiling and is excluded
  from deployable methods and significance.
- Review budgets and abstention capacities are applied per source contract.
  Target capacity affects only the final label-free decision and cannot alter
  source-trained parameters.
- Execution status is explicit. Resource-blocked, not-applicable, and
  abstain-only sentinel scores do not enter predictive ordering.
- The partial GraphSafe surface is honestly named
  `graphsafe_confidence_abstention_component`. The Mowst-inspired
  reimplementation selects and freezes its routing threshold on balanced
  source validation.
- Expert-prediction seed and method-specific router-training seed are separate;
  inference blocks only on expert-prediction seed.
- The frozen gate requires both datasets, all three target contracts, seeds
  1--10, every expert, baseline, ablation, fold, and metric with exact cells.
- Full-versus-ablation effects, Holm decisions, nonzero effect thresholds,
  bootstrap intervals, matched worst-contract improvements, within-seed
  maximum/CVaR regret, two-dataset support, harm limits, routing behavior, and
  target-label freedom all affect the verdict.

## Deterministic validation

The full repository suite passes 176 tests; the anonymous package passes 149.
Critical coverage is contracts 96%, routing 94%, objectives 86%, pilot 87%,
statistics 94%, SupportEngine 88%, gate evaluator 86%, and splits 96%.
Compileall, Ruff, mypy, theory, notebooks, ten deterministic synthetic
mechanisms, one-epoch CPU smoke, placeholder paper build/audit, anonymous
release/audit, a zero-finding staged public-tree audit, analysis freeze, and
the 249-file frozen boundary pass.

The first complete local-gate run exposed that the anonymous allowlist omitted
the two new non-empirical V3 specifications. The allowlist was repaired to
package them under `specifications/`; the final complete gate passes.

## Non-execution boundary

Official baselines were not installed. Datasets were not downloaded. Real
manifests or predictions were not created or connected. The pilot, Kaggle,
multi-seed campaigns, and provider training were not run. PR #2 remains draft
and unmerged. This report does not claim pilot execution readiness.

Remaining external work is manifest conversion, a dry-run exact-completeness
audit, and third independent review before any pilot authorization.
