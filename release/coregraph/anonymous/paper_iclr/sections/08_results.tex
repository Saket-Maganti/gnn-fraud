\section{Results: blocked structure}

\input{tables/primary_results}
\input{tables/worst_contract_results}

\paragraph{Primary comparison.}
\ResultPending{R1-PRIMARY-FRAUD} This paragraph will report per-dataset global target AUPRC and \texttt{contract\_regret\_vs\_feasible\_row\_oracle} only after 180 prediction members, 60 scenarios, 540 bindings, exact 240-coordinate package validation, and Holm-adjusted claim gates pass. No expected winner is stated; the real pilot is unexecuted.

\paragraph{Unseen compositions.}
\ResultPending{R2-COMPOSITION} The protocol one-hot, flat MLP, factorised, interaction, and attention encoders will be compared on held-out combinations and, separately, unseen values. The composition theorem does not license an empirical claim.

\paragraph{Diagnostics and hierarchy.}
\ResultPending{R3-ROUTING} Contract-only, instance-only, and hierarchical routing will be compared jointly on regret and stability. Expert-aware diagnostics must beat contract-only metadata under their frozen gate before the corresponding claim is permitted.

\paragraph{Robustness and abstention.}
\ResultPending{R4-ROBUST-SELECTIVE} Mean-risk, regret, worst/CVaR, and abstention variants will report coverage with selective risk. Zero coverage is not a successful result.

\input{tables/ablations}

All result figures remain unpopulated templates. Their generation script accepts only validated result manifests and never plausible placeholder values. Current blocked claims include superiority, reduction in regret, useful coverage, cross-dataset consistency, resource efficiency, and latent-discovery benefit.
