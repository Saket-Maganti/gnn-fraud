# Manifests

Authoritative manifests remain at the paths consumed by the deterministic
builders:

- `release/tkde_visual_rebuild/tkde_visual_artifact_manifest.csv`;
- `release/tkde_visual_rebuild/tkde_visual_excluded_file_manifest.csv`;
- `release/tkde_visual_rebuild/CHECKSUMS.sha256`;
- `results/tkde_visual_rebuild/FROZEN_SCIENTIFIC_INPUT_HASHES.csv`;
- `docs/SOURCE_TO_GITHUB_PATH_MAP.csv`.

No raw prediction or dataset payload is stored here.

