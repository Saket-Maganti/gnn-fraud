# CoReGraph / ContractShift pre-run system

This directory documents the new ICLR-track research system. It is separate
from the frozen FraudShiftBench/TKDE scientific assets. No file under the
frozen boundary may be modified to support a CoReGraph claim.

The implementation is ready for deterministic smoke and synthetic validation.
The V4 converter and no-training runner/gate audit are implemented. Historical
discovery remains blocked by unresolved contract, role, provenance, and
availability metadata plus missing required cells, so the saved-output pilot
is not execution-ready. A separate fourth independent review must evaluate any
future completed manifests before pilot execution. Provider data and official
external repositories remain explicit inputs; no loader substitutes synthetic
data for a missing dataset.

Start with `RESEARCH_IDENTITY.md`, then `EXPERIMENT_PROTOCOL.md`. Before any
heavy execution, run the local gates in `ICLR_GO_NO_GO_GATES.md`.
