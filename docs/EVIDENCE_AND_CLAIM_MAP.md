# Evidence and claim map

The frozen ledger contains 22 typed claims:

- 12 `SUPPORTED`;
- 3 `DIAGNOSTIC_ONLY`;
- 2 `REFUTED_IN_SCOPE`;
- 2 `RESOURCE_BLOCKED`;
- 2 `SUPPORTED_THEORETICALLY`;
- 1 `SUPPORTED_WITH_RESOURCE_BOUNDARY`.

Each claim records its exact scope, quantifier, comparison, metric, uncertainty
requirement, deployment interpretation, permitted wording, prohibited
generalization, and matched evidence units.

The five evidence families are:

1. real-graph protocol and visibility comparisons on Elliptic and DGraphFin;
2. IBM AML baseline, graph-construction, metric, scale, and resource analyses;
3. bounded GraphSafe saved-score and review-budget analyses;
4. theoretical contract/calibration statements;
5. framework-integrity mutations and false-promotion checks.

Authoritative machine-readable files:

- `results/tkde_rebuild/CLAIM_EVIDENCE_LEDGER.csv`;
- `results/tkde_rebuild/EVIDENCE_INVENTORY.csv`;
- `results/tkde_rebuild/NUMBER_PROVENANCE_MAP.csv`;
- `results/tkde_rebuild/FRAMEWORK_VALIDATION_CASES.csv`;
- `results/tkde_rebuild/FALSE_PROMOTION_AUDIT.csv`;
- `results/tkde_visual_rebuild/FROZEN_SCIENTIFIC_INPUT_HASHES.csv`.

The Markdown ledger provides the compact 22-claim view. Raw prediction payloads
are absent; evidence locks and manifests preserve their counts and hashes.

