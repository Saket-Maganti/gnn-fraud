# Release metadata

This Git tree includes final PDFs, source, aggregate evidence, manifests, and
validation reports. It deliberately excludes the local release ZIPs to avoid
duplicate archives and accidental redistribution of a broader payload.

The exact local archive checksums and excluded-member records remain documented
under `release/tkde_visual_rebuild/`.

